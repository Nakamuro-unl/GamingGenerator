{
  "name": "gaming-generator",
  "version": "1.0.0",
  "description": "Gaming effect generator with GIF animation support",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod"
  }
}